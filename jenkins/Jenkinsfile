pipeline {
    agent any
    stages {
     
        stage('SonarQube analysis') {
            agent { 
                label 'slave'
            }
            steps {
              withSonarQubeEnv('sonar') {
                sh 'mvn clean package sonar:sonar'
              }
            }
        } 
       
       
    }
}
